{
    "name": "kiriminaja",
    "module": "src/index.ts",
    "version": "0.1.0",
    "type": "module",
    "private": true,
    "scripts": {
        "release": "bun test && changelogen --release && git push --follow-tags",
        "test": "bun test",
        "typecheck": "tsc --noEmit"
    },
    "devDependencies": {
        "@types/bun": "^1.3.9",
        "changelogen": "^0.6.2",
        "typescript": "^5.9.3"
    },
    "peerDependencies": {
        "typescript": "^5"
    },
    "dependencies": {
        "zod": "^4.3.6"
    }
}
